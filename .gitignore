# Nix build outputs
result
result-*

# direnv
.direnv/

# Claude Code local settings (settings.json itself stays tracked)
.claude/settings.local.json

# Editor / OS cruft
.vscode/
.idea/
.DS_Store
*.swp

# Secrets safety net (sops-nix files are ciphertext and safe to commit — this just catches accidental plaintext strays)
*.pem
*.age
.env
id_rsa*
id_ed25519*
